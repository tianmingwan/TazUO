# TazUO 全游戏内界面中文本地化 + 繁简转换修复 工作计划

## 一、任务总览

本次整改解决两个独立问题:

| 问题 | 根因 | 方案 |
|------|------|------|
| **繁体没转成简体** | `ChineseCharConverter.cs` 的 `trad`/`simp` 内联字符串被编码污染成乱码(常用繁体字「萬與醜」命中率 0/12) | 改为从已验证正确的 `Resources/TSCharacters.txt`(OpenCC,3767 对,命中率 100%)加载 |
| **游戏内界面还是英文** | 约 600-700 处硬编码英文字面量未接入翻译系统;`Language.Instance`(JSON)系统本身是好的(已验证 Assistant→General 的"视觉配置"是中文) | 把硬编码字面量逐个替换为 `Language.Instance.X.Y`,并在 `Language.cs` 补字段、`Language.ZH.json` 填中文 |

**翻译风格**(已定):品牌名保留英文(Legion Assistant、TazUO),功能词中译。命令名/脚本 API 保留英文。

---

## 二、执行顺序(共 4 个阶段)

> **建议严格按顺序执行**,每个阶段完成后可独立编译验证。繁简转换(P0)和 Legion Assistant 骨架(P1)优先,因为最影响体验。

---

### 🔴 P0:修复繁简转换(独立,优先做完)

**原理**:正确的字典数据 `src/ClassicUO.Utility/Resources/TSCharacters.txt` 已存在且验证正确(OpenCC 格式,`繁<TAB>简`,3767 对)。调用方 `ClilocLoader.cs`(L99/L112/L307)逻辑完全正确,无需改。**只需修 `ChineseCharConverter.cs` 让它从文件加载。**

#### P0.1 修改 `src/ClassicUO.Utility/ClassicUO.Utility.csproj`
在 `</Project>` 前添加,把字典文件配成嵌入资源:
```xml
<ItemGroup>
  <EmbeddedResource Include="Resources\TSCharacters.txt" />
</ItemGroup>
```
当前 csproj 只有 `FNA.Core` 引用和 `SixLabors.ImageSharp`、`System.Text.Json` 两个 PackageReference,无任何 EmbeddedResource,直接追加即可。

#### P0.2 重写 `src/ClassicUO.Utility/ChineseCharConverter.cs`
完全替换文件内容。新实现:
- 静态字段 `_traditionalToSimplified` 保持 `Dictionary<char,char>`
- 静态构造里用 `Assembly.GetExecutingAssembly().GetManifestResourceStream("ClassicUO.Utility.Resources.TSCharacters.txt")` 读取嵌入资源
  - 资源全名规则:默认命名空间(`ClassicUO.Utility`)+ 文件夹(`Resources`)+ 文件名,用 `.` 拼接 → `ClassicUO.Utility.Resources.TSCharacters.txt`
  - ⚠️ 需确认 Utility 程序集的默认命名空间(根命名空间)。若不确定,可在 csproj 加 `<RootNamespace>ClassicUO.Utility</RootNamespace>` 强制。若资源名不对,运行时 `GetManifestResourceStream` 返回 null,需用 `Assembly.GetManifestResourceNames()` 调试实际名字
- 按行读取,跳过空行和 `#` 注释行,按 `\t` 分割,取 `parts[0][0]` 作 key,`parts[1].Split(' ')[0][0]` 作 value(值部分可能含空格分隔的多个候选,取第一个)
- 用 try/catch 包裹解析,出错时 Log.Error 但不抛异常(避免启动崩溃)
- `TraditionalToSimplified(string text)` 方法签名和逐字符查表逻辑**保持不变**(L19-30),确保 `ClilocLoader` 调用方零改动

#### P0.3 验证
- 编译,确认无错
- 运行游戏,选中文+CHS,检查 cliloc 文本(物品名/NPC 对话)是否变成简体
- 可临时在静态构造末尾加 `Log.Trace($"ChineseCharConverter loaded {_traditionalToSimplified.Count} pairs")` 确认加载了 ~3767 对

**注意**:Utility 程序集需要能访问日志。当前文件 `using ClassicUO.Utility.Log`——确认 Log 类在 Utility 程序集内(应该是,否则现有代码编译不过)。

---

### 🟡 P1:Legion Assistant 骨架 + 顶部菜单栏(高频可见,约 40 条)

**目标**:让用户最常打开的两个面板(Legion Assistant、顶部菜单栏)变中文。

#### P1.1 在 `Language.cs` 的 `AssistantLanguage` 类(L711-747)添加字段
在现有 31 个字段后、`Agents` 属性前,添加:
```csharp
// 窗口标题
public string WindowTitle { get; set; } = "Legion Assistant";
// 主 Tab 名
public string TabGeneral { get; set; } = "General";
public string TabAgents { get; set; } = "Agents";
public string TabFilters { get; set; } = "Filters";
public string TabItemDatabase { get; set; } = "Item Database";
public string TabMacros { get; set; } = "Macros";
public string TabSkills { get; set; } = "Skills";
// General 子 Tab
public string SubTabOptions { get; set; } = "Options";
public string SubTabHUD { get; set; } = "HUD";
public string SubTabSpellBar { get; set; } = "Spell Bar";
public string SubTabTitleBar { get; set; } = "Title Bar";
public string SubTabSpellIndicators { get; set; } = "Spell Indicators";
public string SubTabFriends { get; set; } = "Friends";
public string SubTabPathfinding { get; set; } = "Pathfinding";
// Agents 子 Tab
public string SubTabAutoLoot { get; set; } = "Auto Loot";
public string SubTabDress { get; set; } = "Dress Agent";
public string SubTabAutoBuy { get; set; } = "Auto Buy";
public string SubTabAutoSell { get; set; } = "Auto Sell";
public string SubTabBandage { get; set; } = "Bandage";
public string SubTabOrganizer { get; set; } = "Organizer";
public string SubTabStatLock { get; set; } = "Stat Lock";
// Filters 子 Tab
public string SubTabGraphics { get; set; } = "Graphics";
public string SubTabJournalFilter { get; set; } = "Journal Filter";
public string SubTabSoundFilter { get; set; } = "Sound Filter";
public string SubTabMusicFilter { get; set; } = "Music Filter";
public string SubTabSeasonFilter { get; set; } = "Season Filter";
```

在 `TopBarGumpLanguage` 类(L706-709)添加:
```csharp
public string Assistant { get; set; } = "Assistant";
public string LegionScript { get; set; } = "Legion Script";
public string More { get; set; } = "More +";
public string ToggleNameplates { get; set; } = "Toggle nameplates";
public string Tools { get; set; } = "Tools";
public string SpellQuickCast { get; set; } = "Spell quick cast";
public string OpenBoatControl { get; set; } = "Open boat control";
public string NearbyLoot { get; set; } = "Nearby loot";
public string HealthbarCollector { get; set; } = "Healthbar Collector";
public string RetrieveGumps { get; set; } = "Retrieve gumps";
public string XmlGumps { get; set; } = "Xml Gumps";
public string Reload { get; set; } = "Reload";
```

#### P1.2 在 `src/ClassicUO.Client/Configuration/Language.ZH.json` 对应位置填中文
- `Assistant` 对象(L477 起)内补 `"WindowTitle": "Legion Assistant"`(品牌名保留)、`"TabGeneral": "通用"`、`"TabAgents": "代理"`、`"SubTabAutoLoot": "自动拾取"` 等
- `TopBarGump` 对象内补 `"Assistant": "Assistant"`、`"LegionScript": "Legion Script"`、`"More": "更多 +"`、`"Tools": "工具"`、`"SpellQuickCast": "法术快施"` 等
- ⚠️ **关键陷阱**:`Language.Load()` 读 JSON 后会立即 `Save()` 把 Instance 写回 `Data/Language.ZH.json`(bin 目录)。若 bin 里的旧 ZH 文件缺新字段,Save 会用 C# 英文默认值回填,导致翻译丢失。所以**源码 `Language.ZH.json` 必须同步填好**,且 rebuild 后**删除 bin 里的旧 `Language.ZH.json`** 让 `CopyTranslationFiles` 重新复制(SkipUnchangedFiles 按时间戳判断)

#### P1.3 改调用端
- `AssistantWindow.cs:32` `base("Legion Assistant")` → `base(Language.Instance.Assistant.WindowTitle)`
- `AssistantWindow.cs:59-64` `tabs.AddTab("General", ...)` → `tabs.AddTab(Language.Instance.Assistant.TabGeneral, ...)`
- `GeneralTab.cs:10-16`、`AgentTab.cs:10-16`、`FiltersTab.cs:10-14` 同理替换子 Tab 名
- `TopBarGump.cs` 的 L138/161/187/249/251-256/283/347 同理(注意 TopBarGump 在 `Game.UI.Gumps` 命名空间,需 `using ClassicUO.Configuration;`)

#### P1.4 验证
编译运行,切中文,确认:Assistant 窗口标题、6 个主 Tab、各子 Tab、顶部菜单栏按钮都是中文。切英文确认还是原版英文。

---

### 🟢 P2:Legion Assistant 各 Agent/Filters/Macros/Skills 内部内容(约 450 条,工作量最大)

**这是最大的一块**,涉及 ~20 个文件的内部 UI 文本(复选框标签、按钮、表头、提示消息、tooltip)。

#### 组织策略(关键)
参考现有 `BandageAgentLanguage`(L754-789)模式,在 `AgentsLanguage` 类(L749-752)下逐个添加 Agent 语言类:
```csharp
public class AgentsLanguage
{
    public BandageAgentLanguage Bandage { get; set; } = new();
    public AutoLootAgentLanguage AutoLoot { get; set; } = new();
    public DressAgentLanguage Dress { get; set; } = new();
    public AutoBuyAgentLanguage AutoBuy { get; set; } = new();
    public AutoSellAgentLanguage AutoSell { get; set; } = new();
    public OrganizerAgentLanguage Organizer { get; set; } = new();
}
```
然后在文件末尾(L789 后)逐个新建这些类,字段全部带英文默认值。

同理为 Item Database、Filters、Macros、Skills、HUD/SpellBar/TitleBar/SpellIndicator/Friends/Pathfinding 各建语言类,并在 `AssistantLanguage` 下挂载:
```csharp
public ItemDatabaseLanguage ItemDatabase { get; set; } = new();
public FiltersLanguage Filters { get; set; } = new();
public MacrosLanguage Macros { get; set; } = new();
public SkillsLanguage Skills { get; set; } = new();
public GeneralTabLanguage GeneralSub { get; set; } = new(); // HUD/SpellBar 等
```

#### 执行步骤(对每个 Agent/面板重复)
1. 读对应的 `*TabContent.cs` 全文,列出所有硬编码英文字符串
2. 在 `Language.cs` 新建对应语言类,每个字符串一个字段(英文默认值 = 原字面量)
3. 在 `Language.ZH.json` 对应嵌套位置填中文
4. 改 `*TabContent.cs`,把硬编码字符串替换为 `Language.Instance.Assistant.Agents.AutoLoot.XXX`
5. 编译验证

#### 共用字段抽取
- **魔法学校名**(Magery/Necromancy/Chivalry/Bushido/Ninjitsu/Spellweaving/Mysticism/Mastery):CounterBar 和 SpellBar 各出现一套,抽到 `UiCommonsLanguage` 或新建 `SpellSchoolsLanguage` 共享
- **通用按钮**(Add/Cancel/Delete/Import/Export/Save/Yes/No/Clear/Apply/Edit):建议抽到 `UiCommonsLanguage` 避免重复翻译
- **GameActions.Print 提示消息**(如 "Imported loot list!"、"No items found"):归到各 Agent 语言类

#### P2 涉及文件清单(按普查报告)
- Agents: `AutoLootAgentTabContent.cs`(~60)、`DressAgentTabContent.cs`(~45)、`AutoBuyAgentTabContent.cs`(~40)、`AutoSellAgentTabContent.cs`(~38)、`OrganizerAgentTabContent.cs`(~50)
- Item Database: `ItemDatabaseTabContent.cs`(~55)、`ItemDetailMyraWindow.cs`(~35)
- Filters: `GraphicReplacementTabContent.cs`(~45)、`JournalFilterTabContent.cs`(~22)、`SoundFilterTabContent.cs`(~35)、`MusicFilterTabContent.cs`(~33)、`SeasonFilterTabContent.cs`(~12)
- Macros/Skills: `MacrosTabContent.cs`(~28)、`SkillsTabContent.cs`(~20)
- General 子面板: `HudTabContent.cs`(~22)、`SpellBarTabContent.cs`(~25)、`TitleBarTabContent.cs`(~10)、`SpellIndicatorTabContent.cs`(~50)、`FriendsListTabContent.cs`(~12)、`PathfindingTabContent.cs`(~10)

---

### 🔵 P3:其余游戏内 Gump + Script 窗口 + Managers(约 150 条)

#### P3 涉及文件清单
- `JournalGump.cs`(4 分类标签)
- `VersionHistory.cs`(3 条)
- `WorldMapGump.cs`(~5)、`MapGump.cs`(6)
- `GridContainer.cs`(~20)、`CounterBarGump.cs`(~11)、`SpellBar/SpellBar.cs`(~22)
- `ModernOptionsGump.cs` 漏网的(~25,主要是 Wiki 链接 + 个别 checkbox + GenHotKeyDisplay)
- Script 系列: `ScriptManagerWindow.cs`(~40)、`ScriptBrowserWindow.cs`(7)、`ScriptEditorWindow.cs`(1)、`ScriptErrorWindow.cs`(4)
- 其他 MyraWindows: `PromptPopupWindow.cs`(4)、`PersistentVarsWindow.cs`(~18)、`RunningScriptsWindow.cs`(4)、`TazUOChatWindow.cs`(~8)
- Managers: `OrganizerAgent.cs`(~16)、`DressAgentManager.cs`(5)、`MapWebServerManager.cs`(4)

#### 新建语言类
按界面归组,例如 `ScriptingLanguage` 扩展、`JournalGumpLanguage`、`MapGumpLanguage`、`ScriptManagerLanguage` 等,挂到 `Language` 根类。

---

## 三、通用执行规范(所有阶段遵守)

### 3.1 新增字段三步法(每个字符串)
1. **C#**:`Language.cs` 对应类加 `public string XxxYyy { get; set; } = "English default";`
2. **JSON**:`Language.ZH.json` 对应嵌套位置加 `"XxxYyy": "中文"`
3. **调用**:把硬编码 `"English default"` 替换为 `Language.Instance.<路径>.XxxYyy`

### 3.2 命名规范
- 字段名用 PascalCase,语义化(如 `EnableAutoLoot`、`SetGrabBag`、`EntriesHeader`)
- 避免缩写,但可接受 UI 通用词(`Add`/`Cancel`/`Delete`/`Save`)
- JSON 键名必须与 C# 属性名**完全一致**(大小写敏感)

### 3.3 翻译风格(已定)
- 品牌名保留英文:`Legion Assistant`、`TazUO`、`Legion Script`
- 功能词中译:`General`→通用、`Auto Loot`→自动拾取、`Spell quick cast`→法术快施
- 命令名/脚本 API 保留英文:`-dressagent`、`sb`、`info`、LScript API
- 缩写保留:`HP`、`ID`、`FPS`、`HUD`
- 专有名词按 UO 社区惯例:`Magery`→魔法、`Necromancy`→死灵、`Chivalry`→圣骑士

### 3.4 每个阶段后必须做的验证
1. **编译**:`dotnet build`(在 `src/ClassicUO.Client/` 或解决方案根)
2. **清理 bin 旧翻译**:删除 `bin/Debug/net10.0/win-x64/Data/Language.ZH.json`(强制重新复制)
3. **rebuild**:确保 `CopyTranslationFiles` target 把新 ZH json 复制到 bin
4. **运行验证**:切中文,打开对应界面确认中文;切英文确认原版英文;切 CHS 确认繁简转换生效

### 3.5 不要做的事
- ❌ 不要创建英文 `Language.json` 源文件(它是运行时生成的,英文来自 C# 默认值)
- ❌ 不要改 `ClilocLoader.cs`(繁简转换调用逻辑正确)
- ❌ 不要改已经接入 `Language.Instance` 的地方(如 ModernOptionsGump 大部分、GeneralTabContent、BandageAgentTabContent)
- ❌ 不要用 `TazLang.Get(...)` 接入新代码(那是登录界面的老 INI 系统,统一用 `Language.Instance`)
- ❌ 不要碰命令名/脚本 API 的本地化

---

## 四、关键文件索引

### 待修改的核心文件
- `src/ClassicUO.Utility/ChineseCharConverter.cs`(P0,重写)
- `src/ClassicUO.Utility/ClassicUO.Utility.csproj`(P0,加 EmbeddedResource)
- `src/ClassicUO.Client/Configuration/Language.cs`(P1/P2/P3,加字段+类)
- `src/ClassicUO.Client/Configuration/Language.ZH.json`(P1/P2/P3,填中文)
- `src/ClassicUO.Client/ClassicUO.Client.csproj`(已有 CopyTranslationFiles target,无需改)

### 范本参考(已正确接入的代码,照抄模式)
- `src/ClassicUO.Client/Game/UI/MyraWindows/Widgets/Assistant/Agents/BandageAgentTabContent.cs:14`(`Language.Instance.Assistant.Agents.Bandage` 用法)
- `src/ClassicUO.Client/Game/UI/MyraWindows/Widgets/Assistant/GeneralTabContent.cs:18`(`Language.Instance.Assistant` 用法)
- `src/ClassicUO.Client/Game/UI/Gumps/ModernOptionsGump.cs:30`(`Language.Instance.GetModernOptionsGumpLanguage` 缓存为局部变量)

### 数据文件(已存在,正确)
- `src/ClassicUO.Utility/Resources/TSCharacters.txt`(OpenCC 繁简字典,3767 对)

### 调用链(无需改,供理解)
- `src/ClassicUO.Client/Main.cs:46` 和 `Client.cs:110` 调用 `Language.Load()`
- `src/ClassicUO.Assets/ClilocLoader.cs:99/112/307` 调用 `ChineseCharConverter.TraditionalToSimplified`

---

## 五、工作量与时间预估

| 阶段 | 改动量 | 文件数 | 预估难度 |
|------|--------|--------|----------|
| P0 繁简转换 | 2 文件(重写1+改csproj1) | 2 | 低(数据现成,逻辑现成) |
| P1 Assistant 骨架+TopBar | ~40 字符串 | 8(Language.cs/ZH.json + 6 调用端) | 低(模式清晰) |
| P2 Agent/Filter/Macro/Skill 内容 | ~450 字符串 | ~22 | **高(量大,需逐文件处理)** |
| P3 其余 Gump/Script/Manager | ~150 字符串 | ~18 | 中 |
| **合计** | **~640 字符串** | **~50 文件** | |

**建议执行节奏**:P0→P1 一气呵成(快速见效)→ P2 分批(Agents 一批、ItemDB+Filters 一批、Macros+Skills+General子面板 一批)→ P3 收尾。每批做完编译验证一次。

---

## 六、风险与回滚

- **风险1**:Utility 程序集的嵌入资源名不对 → 用 `GetManifestResourceNames()` 调试,或显式设 `<RootNamespace>`
- **风险2**:bin 旧 ZH json 被英文回填 → 每次改完源码 ZH json,删 bin 里的再 rebuild
- **风险3**:某字段 JSON 键名拼错 → 系统.Text.Json 默认大小写敏感,会静默用默认值;改完逐个核对
- **回滚**:所有改动都是新增字段+替换字面量,不破坏现有逻辑。出问题可 git checkout 对应文件单独回滚

---

## 七、切换模型后的启动指引

切换后,直接从 **P0** 开始执行即可。每完成一个阶段,建议运行一次游戏确认效果再继续下一阶段。

另外补充几个在计划中标注了 ⚠️ 但切换后值得优先核实的点:

1. **Utility 程序集的根命名空间** — 决定嵌入资源全名(`ClassicUO.Utility.Resources.TSCharacters.txt` 还是别的)。若运行时 `GetManifestResourceStream` 返回 null,用 `Assembly.GetManifestResourceNames()` 调试实际名字,或在 csproj 显式设 `<RootNamespace>`。
2. **bin 旧 `Language.ZH.json` 回填陷阱** — 每次改完源码 ZH json,务必删除 `bin/Debug/net10.0/win-x64/Data/Language.ZH.json` 再 rebuild,否则 `Language.Load()` 的 `Save()` 会用 C# 英文默认值把翻译覆盖掉。
3. **不要改 `ClilocLoader.cs`** — 繁简转换的调用逻辑(L99/112/307)完全正确,只需修 `ChineseCharConverter` 让字典内容正确即可。
4. **不要用 `TazLang.Get(...)`** 接入新代码 — 那是登录界面的老 INI 系统,统一走 `Language.Instance`。
